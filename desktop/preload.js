// Intentionally minimal. ERP Nexus is a trusted first-party web system, so the
// desktop shell exposes NO extra Node/Electron APIs to the page — it runs with
// contextIsolation + sandbox on. This file exists so the preload path resolves.
