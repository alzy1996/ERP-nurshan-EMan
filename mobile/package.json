{
  "name": "erp-nexus-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "ERP Nexus Android app — a Capacitor shell that loads the live web system, so phone, web and desktop run the exact same app and data.",
  "author": "Eman Works",
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
